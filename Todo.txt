# Current

features: more git suport
	find files changed in last commit, or any commit, in git repo
	find files changed in last commit, or any commit, and only display those files in ftl

	integrate:
		https://github.com/unhappychoice/gitlogue
		https://github.com/modem-dev/hunk
		https://github.com/areese801/git-visual-timeline
		commands that open vim with fugitive command run

	open in tig, gitlogue, git history, fgl, ...
	go to next  git file, ...

	show files that have changes in the last commit, or any commit, in git repo
	eg: ftl -g HEAD~1
	open it in vim + fugitive, gitlogue, ...

feature: https://github.com/tjblackheart/andcli

issue: dir_entries_relative_path_length[$pnc]=0 is not used

functionality: different virtual directory in different tabs
		show all selected
		show results of a search
		result of a diff or other command
		sorting directory in sub virtual directories

functionality: completion for ftl commands
	eg: ftl -v file1 file2 file3
	eg: ftl -v file1 file2 file3 -o output_dir

functionality: multiple files/dirs as arguments
	-v put them  in the same virtual directory
		without directory structure, just the files
	-t in different tabs
	-r recursively add files/dirs to virtual directory

	some of the files/dirs may not exist
		option to create them if they don't exist

functionality: option to create a virtual directory with a list of files, and then use ftl to browse it
	eg: ftl -v file1 file2 file3
	eg: ftl -v file1 file2 file3 -o output_dir

	possibility to put different files in different virtual directories, and then use ftl to browse them
		this can be used to browse files from different categories in git commits


functionality: wizards to add new functionality
	eg: add a new command, add a new key binding, add a new mode, ...
		eg: add a new command to ftl, add a new key binding to ftl, add a new mode to ftl, ...

functionality: replace åäö with other special characters, update documentation

functionality: use mcat as file and image viewer

functionality: inline rename mode
	leader r i: enter mode with keyboard sub mode handler
	escape: cancel mode
	up/down: move through entries
	letters enter rename mode for current entry
		special shortcut to add label to image
		letter add letter
		backspace remove letter
		escape cancel rename mode
		return accept rename
	return: enter rename mode for current entry
	special shortcuts for
		delete entry
		select entry
		batch rename with regexp 
		exit tagging
		sequential rename
		regexp rename

issue: yif not working!
issue: zT not working!

functionality: run on entry in tmux popup pane

issue: inotify not working!

issue: error message fromn inotify
	? used in empty directory

	?fix moved inotify_s down in code

functionality: pane splitting
		what do we want?
			split the current pane
			keep focus or pass to new split
			split on the other side of the preview

# Later

functionality: make command out of fzf-ftl previewer

functionality: grep in binary files https://github.com/phiresky/ripgrep-all
		asciio files are compressed can we grep in them?
		
		only interesting if we're going to select and filter them
		it's probably better to let user generate a selection/filter list
		and instead for adding it to ftl, let the user handle it

functionality: drag drop via dragon, what's the use case?

arch: multi column attributes
	have a column for entry number, size, etags, access attribute, time, file name, ...
	
	user can add and remove, except file
	user can sort per one or more of the columns 
	user can chose which column is displayed and where
	
	columns can be added by background process
		eg: many files need processing and take a long time
			list every time a batch is processed

arch: asynch list display for very large directories
	process1: generate list in background process (filter, color, ...)
	process2: keep the list in memory
	ftl:      query from process2

	optimizations:
		once a directory is processed, setting inotify on it let's us
		update the list and avoid generating the list again
	
	note that ftl already runs process1 in the background 

arch: put all options in one array
	to simplify handling it

arch: let hidden pane do preview so main pane data isn't overridden by extra pane's
	this may also make it possible to use fz, rg, etc... as filters and ftl as preview

