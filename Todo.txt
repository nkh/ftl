issue ?: alt_screen not aware of which screen it is in, so it doesn't know which screen to restore to when exiting alt_screen
	add a mechanism to keep track of the current screen and restore it when exiting alt_screen in bash:
		eg: use a variable to keep track of the current screen and restore it when exiting alt_screen
		using this code:
		```bash
		# Save the current screen
		current_screen=$(tput smcup)
		# Restore the previous screen
		tput rmcup
		```

feature: pick file in fzf and open it directly
		ctrl+0
		other key binding to start the search and open the selected file directly 
		find, latest used, most used, ...

feature: show when a git file was last changed and by whom

	When browsing a Git repository, it would be useful to see when a file was last changed and by whom.

feature: show how many times a file was changed in the last 10 commits

feature: virtual directorie for git based on how many times a file was changed


feature: virtual directories support arbitrary file collections

	Expand virtual directories into a general mechanism for browsing a collection of files

	A virtual directory should be constructible from:

	* individual files
	* directories
	* recursively collected files
	* search results
	* selected files
	* diff results
	* results from other FTL commands
	* files changed by a Git commit
	* files from different categories or sources
	* file/directory that does not necessarily correspond to a physical file
		this is how virtual files work but the content needs to be defined in an ftl script, change
		it so any process can generate a virtual file, not just ftl scripts

	For example:

	```text
	ftl -v file1 file2 file3
	ftl -v file1 file2 file3 -o output_dir
	```

	Support options for controlling how the files are organized:

	* `-v` — add files/directories to a virtual directory
	* `-r` — recursively add files from directories
	* `-t` — put groups of files in separate tabs
	* option to preserve the original directory structure
	* option to flatten the structure and show only the files

	Allow multiple virtual directories to exist simultaneously, including different virtual directories in different tabs.

	This would allow, for example, one tab to contain search results, another to contain files from a diff, and another to contain a manually selected group of files.

	Virtual directories should also support grouping files into subdirectories, allowing files to be sorted into categories within the virtual directory.

	### Missing files

	Some files or directories supplied as arguments may not exist.

	Provide an option to create missing files/directories when constructing the virtual directory.

	### creation time

	can be at start up with arguments or at run time with a command, eg: selected files

	### display

	A virtual directory should be displayed in the same way as a physical directory, with the same options for sorting, filtering, and previewing files.

	files in a virtual directory should be displayed with their original names and paths, even if they are located in different directories or do not exist on disk.

	files in a virtual directory can be displayed with another name or path

	A virtual directory contains files that may not exist on disk or files located in different directories, what they are and where they come from should be displayed
		option or etag

	Sorting and filtering should be available for virtual directories, just like for physical directories.
		sorting is a bit more complicated since the files may come from different directories and have different attributes
			eg: should files be sorted by their original directory
			eg: should files be sorted by their original name or by their virtual name


feature: Add a general filtering mechanism that can be used to restrict what FTL displays.

	Filtering should be usable with:

	* Git commit results
	* search results
	* diff results
	* command output
	* selected files
	* virtual directories

	Ideally, filtering should feed directly into the virtual-directory mechanism so that any file list can become a browsable file collection.

	we have a user filter mechanism, but it is not very flexible as there is just one of them

feature: more Git integration and Git-based virtual directories

	Expand Git support by making Git operations able to produce file collections that can then be browsed through FTL's virtual-directory mechanism.

	### Files changed by a commit

	Add an option to select the files changed by a specific commit and open them in FTL:

	```text
	ftl -g HEAD~1
	ftl -g <commit>
	```

	The resulting files should be displayed as a virtual directory containing only the files changed by that commit.

	This should work for any commit, not only `HEAD`.

	The same mechanism could be used to build virtual directories containing files from different commits or categories, making it possible to compare or browse groups of changed files.

	### Open Git tools from FTL

	Provide commands for opening the current file, selection, or virtual-directory contents in external Git tools, such as:

	* Vim with an appropriate Fugitive command
	* tig
	* git history
	* fgl

	Investigate integration or useful ideas from:

	* [https://github.com/unhappychoice/gitlogue](https://github.com/unhappychoice/gitlogue)
	* [https://github.com/modem-dev/hunk](https://github.com/modem-dev/hunk)
	* [https://github.com/areese801/git-visual-timeline](https://github.com/areese801/git-visual-timeline)

	issue: gvt not displaying properly, maybe because of the way it is called from ftl

feature: https://github.com/tjblackheart/andcli

issue: dir_entries_relative_path_length[$pnc]=0 is not used

functionality: wizards to add new functionality
	eg: add a new command, add a new key binding, add a new mode, ...
		eg: add a new command to ftl, add a new key binding to ftl, add a new mode to ftl, ...

functionality: completion for ftl commands
	eg: ftl -v file1 file2 file3
	eg: ftl -v file1 file2 file3 -o output_dir

functionality: replace åäö with other special characters, update documentation

functionality: use mcat as file and image viewer

functionality: inline rename mode
	leader r i: enter mode with keyboard sub mode handler
	escape: cancel mode
	up/down: move through entries
	letters enter rename mode for current entry
		special shortcut to add label to image
		letter add letter
		backspace remove letter
		escape cancel rename mode
		return accept rename
	return: enter rename mode for current entry
	special shortcuts for
		delete entry
		select entry
		batch rename with regexp 
		exit tagging
		sequential rename
		regexp rename

issue: yif not working!
issue: zT not working!

functionality: run on entry in tmux popup pane

issue: inotify not working!

issue: error message fromn inotify
	? used in empty directory

	?fix moved inotify_s down in code

functionality: pane splitting
		what do we want?
			split the current pane
			keep focus or pass to new split
			split on the other side of the preview

# Later

functionality: make command out of fzf-ftl previewer

functionality: grep in binary files https://github.com/phiresky/ripgrep-all
		asciio files are compressed can we grep in them?
		
		only interesting if we're going to select and filter them
		it's probably better to let user generate a selection/filter list
		and instead for adding it to ftl, let the user handle it

functionality: drag drop via dragon, what's the use case?

arch: multi column attributes
	have a column for entry number, size, etags, access attribute, time, file name, ...
	
	user can add and remove, except file
	user can sort per one or more of the columns 
	user can chose which column is displayed and where
	
	columns can be added by background process
		eg: many files need processing and take a long time
			list every time a batch is processed

arch: asynch list display for very large directories
	process1: generate list in background process (filter, color, ...)
	process2: keep the list in memory
	ftl:      query from process2

	optimizations:
		once a directory is processed, setting inotify on it let's us
		update the list and avoid generating the list again
	
	note that ftl already runs process1 in the background 

arch: put all options in one array
	to simplify handling it

arch: let hidden pane do preview so main pane data isn't overridden by extra pane's
	this may also make it possible to use fz, rg, etc... as filters and ftl as preview

