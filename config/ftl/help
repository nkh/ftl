		j B       ) next entry                           	?         ) show help
		k A       ) previous entry                       	5 6       ) page down/up
		h D       ) parent directory                     	⇈s TAB    ) new tab, nexttab
		l C ''    ) cd or open file
		g G       ) top/file/bottom, cd                  	ö Ö ä     ) entry: next =type, next !type, goto
		a         ) kill sound preview                   	> <       ) extra pane
		b ⇑b ⇈b   ) fzf find/all/dirs                    	_ | ¦     ) extra pane
		c ⇑c      ) copy to, show command mapping        	-         ) next pane or viewer
		d ⇑d ⇈d   ) delete, clear filters/history        	/         ) find
		e E       ) external viewer, detached            	⇈i        ) fzfi, using ueberzurg
		f F ⇑f ⇈f ) filter1/2, external, reverse         	{         ) fzf to file with preview
		H ⇑h ¨ ⇈h ) fzf history, global, global, edit    	}         ) rg to file
		i I ⇑i    ) new file/directory, bulk
		J K       ) scroll preview                              :         ) shell + commands: q, etags, load_tags
		⇑k        ) clear persistent marks               	SPACE     ) leader key
		⇈l ⇑l     ) create/follow symlink
		m         ) mark directory and file              	, ;       ) persistent mark add/fzf
		M ⇑m ⇈m   ) view mode, montage/refresh
		n N       ) find next/previous                   	' ⇑'      ) go, fzf to mark
		o O       ) sort type, reverse                   	)         ) file/dir view mode
		⇑o ⇈O     ) merge tags, fzf                             =         ) preview directory only
		p P ⇈p    ) copy/move selection, move to                *         ) maximum listing depth
		q Q ⇑q ⇈q ) quit, forced, keep shell/ zoom
		r R       ) refresh, rename/bulk rename          	$         ) detach editor preview
		s S       ) shell pane, with files
		⇑s        ) show/hide size                       	!         ) view shell
		t T ⇑t    ) fzf tag, +subdir, to clipboard       	¿         ) pdh pane
		⇈t        ) file preview at end                  	. ⇑. ⇈.   ) show/hide dot-files/etags, etags
		U u ⇑g    ) untag all, fzf, goto                 	^         ) show/hide stat
		v V       ) preview hide, once                   	#         ) show/hide extension preview
		⇑v ⇈v     ) ext. preview mode (media, pdf, tar)  	"         ) show/hide image preview
		w W       ) terminal player, extra viewers       	+         ) change preview size
		x X       ) chmod a+x, a-x                       	% &       ) hide extension, enable all
		y Y ⇑y ⇈y ) tag/untag, files, all                	1-4       ) class tag

		˽fc       ) compress/decompress                  	˽fs       ) stat file
		˽fi       ) reduce image size                    	˽ft       ) terminal popup
		˽fm       ) mail                                 	˽fv       ) reduce video size
		˽fp       ) reduce pdf size                      	˽fx       ) PGP encrypt/decrypt
		˽fP       ) pdf2txt                              	˽fz       ) password encrypt/decrypt
