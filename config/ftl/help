⇑: alt-gr, ⇈: shift+alt-gr, ˽: leader

j B       ) next entry                            q Q @  ) close, > &3, keep shell
k A       ) previous entry                        ?      ) show help
h D       ) parent directory                      5 6    ) page down/up
l C ''    ) cd or open file                       ⇈s \t  ) new tab, nexttab
g G ⇑g    ) first/last entry, cd                  :      ) go to entry
a         ) kill sound preview                    . ⇑.   ) show/hide dot-files/etags
b ⇑b ⇈b   ) find, fzf find/all                    / ⇑/   ) fzf find/find dirs
c         ) copy to                               ⇑i     ) fzfi, using ueberzurg
⇑c        ) compress/decompress                   {      ) fzf to file with preview
d ⇑d ⇈d   ) delete, clear filters/history         }      ) rg to file
e E ⇑e    ) external viewer, detached, extended   ' '    ) leader key
f F ⇑f ⇈f ) filter1/2, external, reverse           
H ⇑h ¨ ⇈h ) fzf history, global, global, edit
i I       ) new file/directory                  
J K       ) scroll preview                        % &    ) disable extension, enable all
⇑k        ) clear persistent marks                , ;    ) persistent mark add/fzf
L         ) symlink selection                  
⇈l ⇑l     ) lock/unlock entry preview
m         ) mark directory and file               ' ⇑'   ) go/fzf to mark
M         ) view mode                             )      ) view file/dir mode
⇑m        ) file preview at end                         
⇈m        ) mail
n N       ) find next/previous                    
o O       ) sort type, reverse                   
⇑o ⇈O     ) merge tags, fzf                       > <    ) extra pane
p P       ) copy/move selection                   _ | ¦  ) extra pane
r ⇑r      ) refresh, refresh montage              -      ) next pane
R         ) rename/bulk rename                    ^      ) show/hide stat
s S       ) shell pane, zoomed                    !      ) run shell command
⇑s        ) show/hide size                        $      ) detach editor preview          
˽s        ) stat file (extra binding)
t T ⇑t ⇈t ) fzf tag,+subdir, all files, all       *      ) maximum listing depth
U u ⇑u    ) untag all, fzf, goto
v V ⇑v    ) preview hide, once, montage           #      ) show/hide extension preview
w W       ) local mplayer, extra viewers          +      ) change preview size
x X       ) chmod a+x, a-x                        =      ) preview directory only
⇑x        ) PGP encrypt/decrypt                   "      ) show/hide image preview
y Y ⇑y    ) tag/untag, to clipboard               :      ) command mode 
Z z       ) force quit, zoom and quit             0      ) refresh
⇑z        ) password encrypt/decrypt              1-9    ) internal usage

commands: q, etags, load_tags
