etag_dir() { : ; }

etag_tag()
{
	local -n r2=$2 r3=$3
	local b1

	r2=
	r3=0
	(( ftl_state_etag_enabled )) && {
		b1="$(basename "$1")"
		((${ftl_plugin_vfiles[$b1]:-0} || ${ftl_plugin_vdirs[$b1]:-0})) && r2='ᵛ ' || r2='  '
		r3=2
	}
}

# vim: set filetype=sh :
