# show_cmd_log — display the command log in a tmux popup
[[ -f "$ftl_state_session_dir/cmd_log" ]] && \
	tmux popup -w 80% -h 80% "$ftl_cfg_markdown_pager $ftl_state_session_dir/cmd_log"

# vim: set filetype=bash :
