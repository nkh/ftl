p="$FTL_CFG/etags" ; file="${1#etags }"

[[ -f "$p/$file" ]] &&
	{ . "$p/$file" $fs ; etag=1 ; ftl::list::change_dir ; } || 
	{ file="$(cd $p ; fd | fzf-tmux -p 50% --reverse --info=inline)" ; [[ "$file" ]] && { . "$p/$file" $fs ; etag=1 ; ftl::list::change_dir ; } ; }

# vim: set filetype=bash :

