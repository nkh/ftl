#!/bin/bash
exec 2>> "$2/generators_log"

f="$(basename "$1")"

[[ FORCE == "$3" || ! -f "$2/$f.jpg" ]] && 
	{
	unrar p -ierr -inull -c- "$1" "$(rar lb "$1" \*.jpg \*.png | perl -ne '! /\d\D\..*$/ and print' | sort | head -n1)" > "$2/$f.large.jpg"
	vipsthumbnail --size 600x "$2/$f.large.jpg" -o "$2/$f.jpg"
	rm "$2/$f.large.jpg" 2>&-
	}
