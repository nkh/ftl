# lib/shell — deprecated, shell-command dispatch is in modules/commands.sh
#
# The shell_command dispatcher, load_sel, and shell_cmd_in_pane functions
# are now defined in etc/core/modules/commands.sh under their new names:
#   ftl::cmd::dispatch_command
#   ftl::sel::load_from_file
#   ftl::cmd::run_command_in_pane
#
# This file is kept as an empty redirect for backwards compatibility.

# vim: set filetype=bash:
